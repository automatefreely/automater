from handler.models.display import Region

class MouseChecker:
	@staticmethod
	def checkClick(region: Region = Region()) -> bool:
		pass
	@staticmethod
	def checkPress(region: Region = Region()) -> bool:
		pass
	@staticmethod
	def checkRelease(region: Region = Region()) -> bool:
		pass